<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Night theme -->
    <style name="Base.Theme.Funny_Cats" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary colors -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorSurface">@color/black</item>
        <item name="colorOnSurface">@color/text_primary_dark</item>

        <!-- Status bar -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Material 3 specific -->
        <item name="materialThemeOverlay">@style/ThemeOverlay.FunnyCats</item>
    </style>

    <style name="Theme.Funny_Cats" parent="Base.Theme.Funny_Cats" />

    <!-- Theme overlay for Material 3 components -->
    <style name="ThemeOverlay.FunnyCats" parent="">
        <item name="cardViewStyle">@style/CardView.FunnyCats</item>
    </style>

    <!-- Custom CardView style -->
    <style name="CardView.FunnyCats" parent="CardView">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
    </style>
</resources>